#include <stdio.h>

int main(){
    char str[100];
    char st[100];
    scanf("%s %s",&str,&st);
    printf(" You entered: %s and %s",str);
    printf("You entered: %s and %s",st);
    return 0;
}